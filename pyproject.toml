[project]
name = "pslogger"
version = "1.0.0"
description = "This is the custom logger for pixelshift"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["structlog>=24.4.0", "logfire>=1.3.0"]
